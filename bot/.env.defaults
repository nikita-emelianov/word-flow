# copy to .env file and fill with your values

# set bot token, you can find it in @GodFather on Telegram
BOT_TOKEN=

# set api host
# to run locally
# API_HOST=http://localhost
# to run with docker use remote server (docker bundle bot-backend doesn't work atm locally, only with remote server)
API_HOST=
